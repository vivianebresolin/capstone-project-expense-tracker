import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  scrollView: {
    flex: 1,
    padding: 20
  },
  pieChartView: {
    marginVertical: 35
  }
});

export default styles;